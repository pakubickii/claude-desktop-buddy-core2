{
  "_comment": "Example PreToolUse hook routing Bash/Write/Edit permissions through tools/buddy_relay to a USB-attached Core2 buddy. Copy the relevant block into .claude/settings.json (project) or ~/.claude/settings.json (global) and restart Claude Code. tools/buddy_relay falls back to a no-op when the buddy isn't plugged in, so it's safe to keep enabled even when working without the device.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/tools/buddy_relay",
            "timeout": 90
          }
        ]
      }
    ]
  }
}
